Docker Compose Demo

The application was deployed using Docker Compose with three services: frontend (nginx) backend (Flask) and database (PostgreSQL).

Containers communicated over a shared network. The frontend served static content, while the backend provided application logic. Meanwhile, storage was configured for the database using Docker volumes. The application successfully ran and was accessible via localhost.
