Image Comparison

The single stage Docker image is larger because it includes all dependencies and build layers. The multi stage image is smaller because it removes unnecessary build components and only includes the runtime environment.
